name: Build Montainer (Stable) Image

on:
  push:
    branches: [main]
    paths:
      - 'cmd/**'
      - 'internal/**'
      - 'frontend/**'
      - 'acceptance/**'
      - 'test/**'
      - 'examples/docker/**'
      - 'go.mod'
      - 'go.sum'
      - 'Dockerfile'
      - '.dockerignore'
      - 'CHANGELOG.md'
      - 'versions/stable.txt'
      - 'versions/stable.url'
      - 'versions/stable.sha256'
      - '.github/workflows/ci.yaml'
      - '.github/workflows/build-test-promote-image.yaml'
      - '.github/workflows/build-montainer-stable-image.yaml'
  workflow_dispatch:
    inputs:
      publish:
        description: Promote the tested candidate to stable release tags
        required: false
        default: false
        type: boolean

permissions:
  contents: read

concurrency:
  group: montainer-stable-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    name: Source quality gates
    permissions:
      contents: read
    uses: ./.github/workflows/ci.yaml

  image:
    name: Build, acceptance-test, and promote stable
    needs: quality
    permissions:
      contents: read
      packages: write
    uses: ./.github/workflows/build-test-promote-image.yaml
    with:
      server_type: stable
      version_file: versions/stable.txt
      registry_image: ghcr.io/wasinuddy/montainer-stable
      acceptance_suites: '["smoke", "lifecycle", "otel-export", "otel-outage", "otel-flush", "backup", "client"]'
      publish: ${{ github.ref == 'refs/heads/main' && (github.event_name != 'workflow_dispatch' || inputs.publish) }}
