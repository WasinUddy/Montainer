# Required. Choose unique credentials before starting the Compose stack.
# MinIO requires a user of at least 3 characters and a password of at least 8.
MINIO_ROOT_USER=
MINIO_ROOT_PASSWORD=

# Optional; the minio-init service creates this bucket automatically.
AWS_S3_BUCKET_NAME=montainer-backups
